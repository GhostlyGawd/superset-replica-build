# RESUME — fresh-chat handoff (Grove / Superset replica)

**Read this + `STATE.json` + `DECISIONS.md` + `PARITY.md` + `RUBRIC.md` + latest `evidence/` to re-derive everything. Do NOT rely on chat history.**

- Product: **Grove** — 1:1 cross-platform OSS replica of Superset (parallel CLI-agent orchestration over isolated git worktrees).
- Repo: `github.com/GhostlyGawd/superset-replica-build` · Linear project "SWARM — Superset Replica" (`a778bfa7-a33c-4069-b217-33169206345d`).
- Workspace: `D:/GitHub Projects/superset-replica-build`. Toolchain: bun, node 24, gh, rg, caddy (all installed). No Docker (PGlite substitutes — ADR-0003).

## Shipped
- **v0.1.0** Phase 0 (recon + architecture + skeleton; 3-OS CI green).
- **v0.2.0** Phase 1 (Grove brand + `@swarm/ui` design system; anti-slop Critic PASS).
- **v0.3.0** Phase 2 (cross-platform **host engine** — worktree isolation, node-pty agent supervision, adapters, PGlite persistence, WS sync, secure Hono+tRPC daemon; parallel-agents proof P01/P02/P04/P10/P11 + real dispatch P03 + lifecycle P07 **green on Windows+macOS+Linux**, run 27536255083; Critic review-3 PASS; zero quarantines). Linear P01-P04/P07/P10/P11 = Done.
- **v0.4.0** Phase 3 (**Desktop client** — Electron + React on `@swarm/ui`, wired to the REAL host: terminal P05, diff/editor P06, open-in-external P08 [host proc], nav + shortcuts/settings P09; host `settings`/`projects` routers, terminal-IO WS). **Green on Windows+macOS+Linux** run 27550630185 (verify matrix + new real-host `e2e (desktop)` job); independent Critic ALL-PASS incl §6.3; axe a11y 0 critical/serious; perf budgets recorded. Linear P05/P06/P08/P09 = Done. _Billing block (ADR-0012) resolved by making the repo public._
- **v0.5.0** Phase 4 (**Mobile-native PWA** `apps/mobile` — installable offline-first, on `@swarm/ui` + the REAL host: QR single-use-code pairing [bearer in IndexedDB] P12, read journeys [detail/agents/read-only diff], touch terminal over the `/terminal` WS + accessory bar, real-agent dispatch, injectManifest SW + Web Push/VAPID; ADR-0014). **Green on Windows+macOS+Linux** run 27567194903 (verify matrix + `e2e (desktop)` + new `e2e (mobile)` job); independent Phase-4 Critic ALL-PASS incl §6.3; axe a11y 0 critical/serious; phone perf budgets PASS. Linear P12 = Done. _On-device push lights up at the Phase-5 secure origin._

## CURRENT — Phase 5 (Platform & self/remote setup, P13), IN PROGRESS → v0.6.0.
Goal (§ roadmap): make Grove trivially standable + reachable from a phone. Scope (see STATE.next_actions[0]): (a) a **one-command cross-platform bootstrap** (TS, no `.sh` on user paths — install deps, init host, write the manifest, start the daemon) on Win/mac/Linux; (b) **desktop installers** via `electron-builder` — NSIS (Windows) / dmg+zip (macOS) / AppImage+deb (Linux); CI builds/typechecks the packaging config (`ELECTRON_SKIP_BINARY_DOWNLOAD`; actual GUI launch + installer smoke verified locally, not CI); (c) the **phone-only remote path** — `cloudflared`/`localtunnel` + **Caddy internal TLS** so the PWA reaches the host over HTTPS from anywhere → **on-device SW install + Web Push light up** (closes the ADR-0014 dec-4 deferral); (d) QR pairing over the tunnel URL. Gate: independent Phase-5 Critic + evidence (installer artifacts list, a remote-pairing trace) → cut **v0.6.0** + Linear P13 (GHO-377) & P14 (GHO-378) Done. Architecture recorded: **ADR-0015** (bootstrap), **ADR-0016** (electron-builder; CI `--dir`, installers local), **ADR-0017** (cloudflared quick-tunnel + tunnel-URL pairing → on-device push; Caddy self-signed TLS only for the local secure-context e2e).
- **Wave progress (ADR-0017 plan):** **W1 daemon lifecycle ✅** (fa0ad2d; real `grove start` [detached Node daemon + unref], `grove stop` [manifest-PID graceful→tree-kill, taskkill /T on Win], `grove status` [PID liveness + /healthz]; idempotent; `GROVE_HOME` test seam; lifecycle round-trip test 4/4; clean-install cold gate 11/11). → **W2 `grove up` ✅** (aa2ee75; one-command bootstrap = cross-platform dep preflight [`execFile <bin> --version`, no where/which; node24/bun/git required, cloudflared optional] + idempotent `start` [host creates store/bearer/VAPID on boot] + mint+print pair QR; `--check` preflight-only; CLI tests 12/12 incl a real up→status→stop round-trip; clean-install cold gate 11/11). → **W3 remote path ✅** (414340f + test-hardening 6059fc1): `tunnel.ts` spawns cloudflared quick-tunnel (localtunnel MIT fallback via `lt`), parses the trycloudflare/loca.lt https URL, `stop()` tree-kills; `grove pair/up --remote` set pairing endpoint = tunnel HTTPS URL + print the QR (`<tunnelUrl>/?code=`; bearer never in QR); optional host `GROVE_ALLOWED_ORIGINS` allowlist (bearer still the gate); shared `proc.ts` tree-kill. Tunnel unit-tested via a stub-binary seam (URL parse + teardown), CLI tests 18/18. **Gate flake fixed:** real-process suites' hooks/probes flaked at bun's 5s default under default-concurrency contention → generous 60s timeouts (test-only, no assertions changed) → 3× `turbo test --force` deterministic; clean-install cold gate 11/11 / 216 tests. (Real public tunnel + secure-context push = W5/W6 evidence.) → **W4 electron-builder packaging ✅** (800d547; `apps/desktop/electron-builder.yml` appId `sh.grove.desktop`, targets NSIS / dmg+zip / AppImage+deb, unsigned [mac.identity:null, no notarize]; `files`=dist/**+package.json, node_modules excluded [main/preload/renderer bundled]; brand icons `build/icon.{ico,icns}` + `build/icons/*.png` via a png2icons[MIT] generator; `build:installer`/`package:dir` scripts; `PACKAGING.md` SmartScreen/Gatekeeper notes; `main.ts` packaged path already correct. Static-validated [28 checks] — real `--dir` build is W5 since the Electron binary isn't local. Clean-install cold gate 11/11). → **W5 CI evidence (ACTIVE)**: a `.github/workflows/ci.yml` `package` job = `electron-builder --dir` on all 3 OS (UNSET `ELECTRON_SKIP_BINARY_DOWNLOAD` for that job so Electron downloads; `--dir` = unpacked, no installer/GUI) to validate packaging cold; + a Caddy self-signed-TLS phone-viewport e2e proving SW-register + `pushManager.subscribe` over HTTPS (`ignoreHTTPSErrors`). → W6 local evidence (full installers + Win GUI launch screenshot/trace [closes P14] + a real cloudflared remote-pairing trace under evidence/phase-5/) + independent Phase-5 Critic → v0.6.0.
- Then **Phase 6** (hardening/launch → v1.0.0 per §9): full e2e green 3 OSes; **license-audit** (whole-product, ADR-0008); **cross-platform perf re-measure** (desktop terminal-stream p95 tail = Windows-PowerShell variance; re-measure on Linux/macOS); **a11y sweep** — fix the systemic `text-fg-subtle`-on-raised/overlay token (extend `tokens.test.ts`), the closed-`@swarm/ui`-overlay full-bleed quirk (CLOSED `Dialog`/`Sheet` paints full-bleed — consumers conditional-mount; real fix = hide when not `[open]`; hit in B2/W3), and `TerminalFrame`'s inert mobile Find/Split buttons; security; docs incl the phone-only path + `docs/demo.md`; **HANDOFF.md**.
- Harness follow-ups to route via /retro at a clean boundary: the CI-gate lessons + the billing-diagnosis rule + the re-gate-after-evidence rule (in recursive-harness state/followups).
- Discipline reminders: REAL host (no mocks on user paths); record decisions in DECISIONS.md (never ask); delegate to subagent waves (return ≤40 lines + paths); no banned tokens; **gate every pushed HEAD with a clean install + confirm the 3-OS CI run** (cold `--force` ≠ CI ≠ clean-install; and re-gate AFTER adding evidence/QA specs — a QA spec slipped a TS error past a feature-scoped gate in Phase 3, caught only by the Critic).
- Note: the desktop shell intentionally does not reflow below ~16rem — that's fine for a desktop client; the phone experience is Phase 4's job. Open follow-ups to route via /retro: the billing-misdiagnosis rule + the re-gate-after-evidence rule (see harness state/followups).

## Remaining roadmap (§4)
- **Phase 4** Mobile-native PWA (installable, offline-first, Web Push/VAPID, gestures, safe-area, 60fps; full agent-orchestration control from the phone) → v0.5.0.
- **Phase 5** Platform & self/remote setup (one-command bootstrap on Win/mac/Linux; Electron installers NSIS/dmg/AppImage via electron-builder; phone-only remote path; cloudflared/localtunnel + Caddy) → v0.6.0.
- **Phase 6** Hardening & launch: full e2e green on 3 OSes; **produce the §6.2/§6.4 reports the Phase-2 Critic flagged as missing — performance report + recorded speed budgets (terminal-stream latency, interaction latency, cold start) + a license-audit report**; accessibility; security; docs incl phone-only path; `docs/demo.md`; **HANDOFF.md** → cut **v1.0.0** when all §9 exit conditions pass with evidence.

## Hard-won gate discipline (ADR-0011 + addendum)
- **Local `turbo --force` ≠ CI.** Gate app/desktop changes with a **CLEAN install** (rm node_modules + `*.tsbuildinfo` + `.turbo`, `bun install --frozen-lockfile`, then CI's exact steps). Always confirm the 3-OS CI run after pushing.
- Monorepo: explicit `.ts` import extensions + `allowImportingTsExtensions` (Node strip-types workers).
- Windows engine discipline: spawn agent processes **directly** in the PTY (node-pty) with `onExit`; resolve `node`→`process.execPath` (no PATH/`where` in spawned workers); run **batch** commands via `child_process` (not a PTY); test workers flush-then-`process.exit`; bounded `fs.rm({force,maxRetries})` + async-spawn/tree-kill; never rely on Node executing `.ts` or a shell forwarding a child's stdout through ConPTY on the GH Windows runner.
- Playwright: on Windows run via **node, not bun**; CI must `playwright install` browsers before any e2e. Electron: `ELECTRON_SKIP_BINARY_DOWNLOAD=1` in CI (build/typecheck only; GUI launch verified locally + packaged Phase 5).
- Every phase ends with an **independent Critic (fresh context, did NOT build it)**; for feature-UI phases include the §6.3 design bar + screenshots. No banned tokens; no mocks on user happy paths.

## Harness (recursive-harness) housekeeping
- Open predictions to score once resolved: `ec5bd054` (desktop foundation), `0dce6bdf` (B1 terminal/diff), `4927acf5` (B2 nav). Score with `python <harness>/bin/harness outcome <id> --result hit|miss --notes "..."`.
- Follow-up `7c07bf` (and queued in this file): run `/retro` → `/harness-pr` to route the CI-gate lessons (turbo-cache false-green → `--force`; **`--force` ≠ CI → clean-install gate**; Windows ConPTY discipline; fresh independent Critics catch fakes) into harness artifacts. Do at a clean phase boundary.
