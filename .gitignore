node_modules/
dist/
build/
# Committed electron-builder buildResources (icons) — re-included despite build/ above.
!apps/desktop/build/
!apps/desktop/build/**
out/
.turbo/
.cache/
coverage/
*.log
.DS_Store
Thumbs.db

# local env / secrets
.env
.env.*
!.env.example

# embedded db data
.data/
*.db
*.sqlite

# electron build artifacts
release/
*.exe
*.dmg
*.AppImage
*.deb
*.nsis

# test/e2e
playwright-report/
test-results/
.playwright/

# evidence raw captures kept, but ignore large traces by default (committed selectively)
evidence/**/*.zip
evidence/**/*.webm
