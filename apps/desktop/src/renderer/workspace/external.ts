/** Open-in-external targets (P08), mirrored from the host `workspaces.openExternal`
 *  input so the renderer's UI stays decoupled from the host package at runtime. */
export type ExternalTarget = "editor" | "terminal" | "folder";
